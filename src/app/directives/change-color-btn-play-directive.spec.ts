import { ChangeColorBtnPlayDirective } from './change-color-btn-play-directive';

describe('ChangeColorBtnPlayDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeColorBtnPlayDirective();
    expect(directive).toBeTruthy();
  });
});
