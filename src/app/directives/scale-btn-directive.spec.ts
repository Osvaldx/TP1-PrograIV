import { ScaleBtnDirective } from './scale-btn-directive';

describe('ScaleBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleBtnDirective();
    expect(directive).toBeTruthy();
  });
});
