import { SelectedAnswer } from './selected-answer';

describe('SelectedAnswer', () => {
  it('should create an instance', () => {
    const directive = new SelectedAnswer();
    expect(directive).toBeTruthy();
  });
});
