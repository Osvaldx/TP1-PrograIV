import { SelectedCategory } from './selected-category';

describe('SelectedCategory', () => {
  it('should create an instance', () => {
    const directive = new SelectedCategory();
    expect(directive).toBeTruthy();
  });
});
