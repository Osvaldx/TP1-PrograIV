import { SnakeCell } from './snake-cell';

describe('SnakeCell', () => {
  it('should create an instance', () => {
    const directive = new SnakeCell();
    expect(directive).toBeTruthy();
  });
});
