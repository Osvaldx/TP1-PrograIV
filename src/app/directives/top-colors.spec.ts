import { TopColors } from './top-colors';

describe('TopColors', () => {
  it('should create an instance', () => {
    const directive = new TopColors();
    expect(directive).toBeTruthy();
  });
});
